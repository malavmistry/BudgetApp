# Dependency Injection

- Register application services in `Program.cs` through a central service-registration method.
- Depend on interfaces rather than concrete service implementations.
- Add a new interface before adding a new service implementation.