# Task System

Use this folder to track active and completed engineering work when task memory needs to persist across agents or sessions.

## Rules

- Create a task record for multi-step work, cross-agent work, or work that spans sessions.
- Move completed task records from `active/` to `completed/` when finished.
- Keep task records factual and implementation-focused.