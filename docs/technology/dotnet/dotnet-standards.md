# .NET Standards

- Follow nullable reference type rules.
- Use async EF Core APIs for database I/O.
- Keep DI registration centralized in `Program.cs`.
- Preserve framework-consistent naming and handler patterns.